{
  "private": true,
  "scripts": {
    "build": "bsb -make-world",
    "build:watch": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "test": "yarn build && jest",
    "test:watch": "jest --coverage --watchAll",
    "watch": "concurrently 'yarn build:watch' 'yarn test:watch'"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@glennsl/bs-jest": "^0.5.1",
    "bs-platform": "^8.0.3",
    "concurrently": "^5.2.0",
    "jest": "^26.1.0"
  }
}
